# MongoDB (uses .env first, falls back to local if missing)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/brickatime}

# BrickLink API
bricklink.api.url=https://api.bricklink.com/api/store/v1
bricklink.api.token=${BL_TOKEN}